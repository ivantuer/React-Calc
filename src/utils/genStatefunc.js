export default (
    operator,firstNum,currentInput,
    isOperatorClicked = true,isDotClicked = false
    ) => ({
        operator,
        firstNum,
        currentInput,
        isOperatorClicked,
        isDotClicked
    })